```yaml
apiVersion: policy/v1
kind: PodDisruptionBudget

metadata:
  name: django-pdb
  namespace: default

  labels:
    app: django
    component: availability

spec:
  # Always keep at least one Django pod available
  minAvailable: 1

  selector:
    matchLabels:
      app: django
```

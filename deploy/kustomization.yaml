

secretGenerator:
  - name: db-password
    literals:
      - password=samplepassword123
  - name: django
    literals:
      - secret=secretkey123
resources:
  # - postgres.yaml
  - StorageClass.yaml
  - django.yaml
